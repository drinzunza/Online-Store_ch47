Depending on the computer 
py
python3 <-- if youre running a Mac (100%)
python

to install the virtualenv 
py -m pip install virtualenv

to create the virtualenv
py -m venv nameOfTheFolder 

we need to be placed in the folder that contains the venv
windows venv\Scripts\activate
Mac   source venv/bin/activate

//This is for the react project
to create a react project we need to use the following intruction
npx create-react-app "name of the folder"
if for some reason this doesnt work 
try 
npm i -g npx and then npx create-react-app "name of the folder" 

to run the server WE NEED TO BE LOCATED IN TO THE FOLDER THAT WE JUST create
use: npm start

to change directory you need to use -- cd 
